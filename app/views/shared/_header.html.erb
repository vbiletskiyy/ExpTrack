<nav class="navbar navbar-expand-md navbar-dark fixed-top text-dark bg-secondary">
  <div class="container">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <%= link_to 'Dashboard', root_path, class: 'nav-link' %>
      </li>
      <% if current_user %>
        <li class="nav-item active">
          <%= link_to 'Spendings', spendings_path, class: 'nav-link' %>
        </li>
      <% end %>
    </ul>
  </div>
  <% if current_user %>
    <%= link_to 'Log out', logout_users_path, class: 'btn btn-outline-light' %>
  <% else %>
    <%= link_to 'Sign in', login_users_path, class: 'btn btn-outline-light' %>
  <% end %>
</nav>
